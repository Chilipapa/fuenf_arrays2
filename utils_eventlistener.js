export function funktion_01() {
  // hier wird der ausgegraute BUTTON wieder nutzbar
  document.getElementById("saveButton").disabled = false;
}

export function funktion_02() {
  // hier wird der ausgegraute BUTTON wieder nutzbar
  document.getElementById("saveButton").disabled = true;
}
